{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the main window. Extend only with the narrowest permission a feature needs.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
